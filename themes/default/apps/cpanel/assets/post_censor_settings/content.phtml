<div class="cp-app-container" data-app="posts-censor-settings">
    <div class="current-page-name">
        <div class="lp">
            <h1>
                Posts censor settings
            </h1>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <div class="card">
                <div class="header">
                    <h2>
                        Manage posts censored words settings
                    </h2>
                </div>
                <div class="body">
                    <form class="form" data-an="form">
                    	<div class="form-group" data-an="firebase_api_key-input">
                            <label>
                                Censored words (List separated by commas. E.g word1, word2, word3, etc...)
                            </label>
                            <div class="form-line">
                            	<textarea name="words_list" class="form-control" placeholder="Enter censored words list separated by commas"><?php echo($cl["post_censored_words_csv"]); ?></textarea>
                            </div>
                        </div>
                        <div class="form-group">
                            <label>
                                Word replacements (Replace a specific word with a custom word or phrase instead of masking it with asterisks)
                            </label>
                            <div data-an="replacements-list">
                                <?php if (not_empty($cl['post_censored_words_replacements'])): ?>
                                    <?php foreach ($cl['post_censored_words_replacements'] as $cl['li']): ?>
                                        <div class="input-group mb-2" data-an="replacement-row">
                                            <input type="text" class="form-control" name="replacement_words[]" placeholder="Word to replace" value="<?php echo(htmlspecialchars(fetch_or_get($cl['li']['word'], ''), ENT_QUOTES, 'UTF-8')); ?>">
                                            <input type="text" class="form-control" name="replacement_texts[]" placeholder="Replace with" value="<?php echo(htmlspecialchars(fetch_or_get($cl['li']['replacement'], ''), ENT_QUOTES, 'UTF-8')); ?>">
                                            <div class="input-group-append">
                                                <button type="button" class="btn btn-danger" data-an="remove-replacement-btn">
                                                    Remove
                                                </button>
                                            </div>
                                        </div>
                                    <?php endforeach; ?>
                                <?php endif; ?>
                            </div>
                            <button data-an="add-replacement-btn" type="button" class="btn btn-secondary btn-sm">
                                Add word
                            </button>
                        </div>
                       <div class="form-group no-mb">
                       		<button data-an="submit-ctrl" type="submit" class="btn btn-primary">
                                Save changes
                            </button>
                       </div>
                        <input type="hidden" class="d-none" value="<?php echo fetch_or_get($cl['csrf_token'],'none'); ?>" name="hash">
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
<?php echo cl_template('cpanel/assets/post_censor_settings/scripts/app_master_script'); ?>